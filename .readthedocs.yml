# .readthedocs.yml

version: 2

sphinx:
  configuration: docs/conf.py

# Build settings
build:
  os: ubuntu-22.04
  tools:
     python: "3.9"

python:
  install:
    - requirements: docs/requirements.txt

